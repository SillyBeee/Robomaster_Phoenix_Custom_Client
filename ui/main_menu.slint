export component MainMenu inherits Rectangle {
    width: 100%;
    height: 100%;
    background: white; // 给菜单一个背景色，防止透出底下的黑色

    Text {
        text: "Main Menu";
        horizontal-alignment: center;
        vertical-alignment: center;
        font-size: 32px;
        color: black;
    }
}

